# Gradle daemon JVM configuration (requires Gradle 8.8+).
# Pins the JDK used to run the Gradle daemon to Java 17, matching JVM_TOOLCHAIN
# in gradle.properties.  Prevents build failures when the system default JDK is
# newer than what the embedded Kotlin version supports.
toolchainVersion=17
toolchainVendor=adoptium
